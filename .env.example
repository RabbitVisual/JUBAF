APP_NAME="JUBAF"
APP_ENV=local
APP_KEY=base64:QT+30fWe4qhinDGX+tZzbwczau7GLL+FTiF8iL8dy6s=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=pt_BR
APP_FAKER_LOCALE=pt_BR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=jubaf_dev
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_DRIVER=log
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
# Em produção prefira redis + worker dedicado: php artisan queue:work
QUEUE_CONNECTION=database

CACHE_STORE=file

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# Vite Environment Variables
VITE_APP_NAME="${APP_NAME}"

# Module Generation
MODULE_VENDOR=vertex
MODULE_AUTHOR_NAME='Reinan Rodrigues'
MODULE_AUTHOR_EMAIL=r.rodriguesjs@gmail.com
MODULE_AUTHOR_COMPANY='© 2026 Vertex Solutions'
MODULE_AUTHOR_YEAR=2026

# --- Operação / produção (checklist) ---
# - php artisan migrate --force && php artisan storage:link
# - Worker de filas (supervisor/systemd) + monitorizar tabela failed_jobs
# - Backup: base de dados + storage (PDFs de atas, uploads públicos)
# - Rotas /transparencia: throttle 120 req/min por IP (routes/web.php)
